# Placeholder so the embed_frontend build tag has something to embed even
# before `make frontend-build` has run. The Makefile's build-release target
# replaces the contents of this directory with the SvelteKit static build.
